{
  "buildCommand": "cd frontend && rm -rf node_modules package-lock.json && npm install --legacy-peer-deps --no-omit=optional && npm run build",
  "outputDirectory": "frontend/dist",
  "devCommand": "cd frontend && npm run dev",
  "installCommand": "cd frontend && rm -rf node_modules package-lock.json && npm install --legacy-peer-deps --no-omit=optional",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "VITE_SUPABASE_URL": "@vite_supabase_url",
    "VITE_SUPABASE_ANON_KEY": "@vite_supabase_anon_key",
    "VITE_DEFAULT_MODE": "demo"
  }
}

